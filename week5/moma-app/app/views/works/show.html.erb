
<h2>
  <em>
    <%= @work.title %>,
  </em>
  <%= @work.year %>
</h2>
<h4>
  <%= link_to @work.artist.name, artist_path(@work.artist) %>
  (b. <%= @work.artist.dob.year %>)
</h4>

<p>
  <strong>Medium:</strong>
  <%= @work.medium %>
</p>

<p>
  <strong>Style:</strong>
  <%= @work.style %>
</p>

<%= image_tag @work.image, alt: @work.title, class: 'artwork' %>
